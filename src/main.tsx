import { StrictMode } from 'react'
import { createRoot } from 'react-dom/client'
// import App from './App.tsx'
// import { makeServer } from './server.tsx' 



// if (process.env.NODE_ENV === 'development') {  //Cannot find name 'process'. Do you need to install type definitions for node? Try `npm i --save-dev @types/node` and then add 'node' to the types field in your tsconfig.ts(2591)
//   makeServer({ environment: 'development' })
// }
// if (import.meta.env.MODE === 'development') { 
//   makeServer({ environment: 'development' })
// }

// createRoot(document.getElementById('root')!).render(
//   // <StrictMode>
//   //   <App />
//   // </StrictMode>,
// )



// if (import.meta.env.DEV) {
//   makeServer({ environment: 'development' })
// }

  //makeServer({ environment: 'development' })